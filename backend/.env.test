# Test Environment Variables
NODE_ENV=test
LOG_LEVEL=error

# Test Database (use separate DB for tests)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/whatsapp_campaigns_test"

# Redis (use separate DB for tests)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# JWT
JWT_SECRET=test_secret_key_for_testing_only
JWT_EXPIRES_IN=1h

# Twilio (mock in tests)
TWILIO_ACCOUNT_SID=test_account_sid
TWILIO_AUTH_TOKEN=test_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+15555555555
TWILIO_SMS_NUMBER=+15555555555

# OpenAI (mock in tests)
OPENAI_API_KEY=test_openai_key

# DRY_RUN for tests
DRY_RUN=true

# Frontend
FRONTEND_URL=http://localhost:3000
PORT=3002
